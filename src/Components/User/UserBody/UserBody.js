import React from 'react';
import './userBody.css'

const UserBody = () => {
    return (
        <div className='userBodyWrapper'>
            im user body and
            i need to have: profile picture, posts num, followers num, following num, edit profile button and desc.
        </div>
    );
};

export default UserBody;